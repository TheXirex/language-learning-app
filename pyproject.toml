[project]
name = "language-learning-app"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "scrapy==2.17.0",
    "pymongo>=4.6.0",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = [
    "pytest==9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
