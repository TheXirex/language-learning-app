[project]
name = "language-learning-app"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "scrapy==2.17.0",
]

[dependency-groups]
dev = [
    "pytest==9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["src/modules/scrapper"]
testpaths = ["tests"]
